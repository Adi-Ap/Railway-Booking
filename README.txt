MT2021006 Railway Ticket System

Compile the server program using : gcc -o server server.c
Compile the client program using : gcc -o client client.c

Run the Client using: ./client
Run the server using: ./server

Types of Users:
	Admin
	Agent
	Customer

First Sign Up as Admin and add trains.
The key for the admin is "IIITB".

Sign Up and Login as Customer/Agent to book tickets.